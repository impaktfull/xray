---
title: xRay (inspector)
description: A comprehensive Flutter debugging toolkit for inspecting network calls, logs, and key-value storage in development.
---

# xRay (inspector)

A comprehensive Flutter debugging toolkit that captures and displays network calls, logs, and key-value storage — all inspectable in real-time via a remote UI.

## What's included

| Package                             | Description                                                         |
| ----------------------------------- | ------------------------------------------------------------------- |
| `xray_inspector`                    | Core package — network, log, and key-value inspectors + HTTP server |
| `xray_network_dio_interceptor`      | Dio HTTP interceptor                                                |
| `xray_shared_preferences_inspector` | SharedPreferences key-value inspector                               |
| `xray_secure_storage_inspector`     | flutter_secure_storage inspector                                    |
| `xray_inspector_ui`                 | Flutter UI that connects to a running inspector server              |
| `xray_inspector_core`               | Pure Dart models and protocol constants (no Flutter dependency)     |

## Architecture overview

```
Your Flutter app
       │
       ├── XRayNetworkInspector   ◄── ImpaktfullNetworkDioInterceptor
       ├── XRayLogInspector
       ├── XRaySharedPreferencesInspector
       └── XRaySecureStorageInspector
                        │
              XRayInspectorServer  (HTTP + WebSocket + UDP discovery)
                        │
              XRayInspectorUiController  (separate app or debug panel)
                        │
                Inspector UI screens
```

The inspector server runs inside your app and broadcasts data over HTTP/WebSocket. A separate UI (another device, simulator, or debug panel) connects to it via `XRayInspectorUiController`.

## Design principles

- **No singletons** — all dependencies are passed explicitly via constructors.
- **ValueNotifier reactivity** — all data exposed as `ValueNotifier` for Flutter rebuilds.
- **No disk I/O** — all captured data lives in memory only.
- **Composable** — add only the inspectors you need.

## Quick links

- [Quick Start](/getting-started)
- [License](/license)
- [Network Inspection](/guides/network-inspection)
- [Log Inspection](/guides/log-inspection)
- [Key-Value Inspection](/guides/key-value-inspection)
- [Remote UI](/guides/remote-ui)
